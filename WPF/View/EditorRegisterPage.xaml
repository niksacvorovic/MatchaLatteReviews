<Window x:Class="MatchaLatteReviews.WPF.View.EditorRegisterPage"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Register" Height="950" Width="1200"
        Icon="/WPF/img/logo.ico"    
        WindowStartupLocation="CenterScreen"
        Background="White">

    <Window.Resources>
        <Style x:Key="HoverButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1A1A1A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="8,0"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="8,0"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="DemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="40,40,25,0" Height="800" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,20,0,30">
            <Image Source="/WPF/img/logo.png" Width="70" Height="70" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Register New Music Editor"
                       FontSize="30"
                       FontWeight="Bold"
                       Foreground="Black"
                       HorizontalAlignment="Center"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Width="400" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0" >
            <Label Content="Username" FontWeight="Bold" FontSize="16"/>
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Height="35" Margin="0,0,0,15"/>
            <Label Content="First Name" FontWeight="Bold" FontSize="16"/>
            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Height="35" Margin="0,0,0,15"/>
            <Label Content="Last Name" FontWeight="Bold" FontSize="16"/>
            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Height="35" Margin="0,0,0,15"/>
            <Label Content="Password" FontWeight="Bold" FontSize="16"/>
            <PasswordBox PasswordChanged="OnPasswordChanged" Height="35" Margin="0,0,0,15"/>
            <Label Content="Genres" FontWeight="Bold" FontSize="16"/>
            <ComboBox ItemsSource="{Binding Genres}" 
          SelectedItem="{Binding SelectedGenre}" 
          DisplayMemberPath="Genre.Name"
          Height="30" Margin="0,5,0,5"/>
            <Button Content="Add genre"  Style="{StaticResource HoverButtonStyle}"
        Command="{Binding AddGenreCommand}" 
        Height="30" Width="120" 
        Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding SelectedGenres}" 
         Height="43" Margin="0,0,0,15">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,40,0,0">
            <Button Content="Register"
                    Style="{StaticResource HoverButtonStyle}"
                    Command="{Binding RegisterCommand}"
                    Width="200"/>
        </StackPanel>
    </Grid>
</Window>
