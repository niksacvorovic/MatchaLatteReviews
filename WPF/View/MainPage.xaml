<Window x:Class="MatchaLatteReviews.WPF.View.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      d:DesignHeight="900" d:DesignWidth="1500"
      Icon="/WPF/img/logo.ico"
      Title="MainPage">

    <Window.Resources>
        <Style x:Key="HoverButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1A1A1A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="White" 
                Grid.Row="0"
                CornerRadius="0"
                Padding="0">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.3" ShadowDepth="4" BlurRadius="10"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="577*"/>
                    <ColumnDefinition Width="Auto" MinWidth="348"/>
                    <ColumnDefinition Width="450*"/>
                    <ColumnDefinition Width="125*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Height="50" Width="346" >
                    <Image Source="/WPF/img/logo.png"
                           Width="50"
                           Height="50"
                           Stretch="Uniform"
                           Margin="0,0,10,0"/>
                    <TextBlock Text="Matcha Latte Reviews"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="Black"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Margin="0,0,30,0"
            Height="55"
            Width="310"
            Grid.ColumnSpan="2">
                    <Button Style="{StaticResource HoverButtonStyle}" Content="Login" Height="40" VerticalAlignment="Center" Click="Login_Click"/>
                    <Button Style="{StaticResource HoverButtonStyle}" Content="Sign up" Height="40" VerticalAlignment="Center" Margin="10,0,0,0" Click="Register_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        <ScrollViewer Grid.Row="1" Margin="20">
        </ScrollViewer>
    </Grid>
</Window>
