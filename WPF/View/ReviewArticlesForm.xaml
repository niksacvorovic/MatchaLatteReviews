<Window x:Class="MatchaLatteReviews.WPF.View.ReviewArticlesForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MatchaLatteReviews.WPF.View"
        mc:Ignorable="d"
        Title="ReviewArticlesForm" Height="400" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding ArticlesToReview}" SelectedItem="{Binding SelectedArticle}">

        </ListBox>
        <Button Grid.Row="1" Margin="0 10" Click="ViewArticles_Click">
            View Article
        </Button>
        <Button Grid.Row="2" Margin="0 10" Command="{Binding ApproveArticleCommand}">
            Approve Article
        </Button>
        <Button Grid.Row="3" Margin="0 10" Command="{Binding DeclineArticleCommand}">
            Decline Article
        </Button>
    </Grid>
</Window>
