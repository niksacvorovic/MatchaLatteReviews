<Window x:Class="MatchaLatteReviews.WPF.View.AddArtistForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MatchaLatteReviews.WPF.View"
        mc:Ignorable="d"
        Title="AddArtistForm" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Add Artist" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name/Title:" VerticalAlignment="Center" Margin="0,6,10,6"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Rating:" VerticalAlignment="Center" Margin="0,6,10,6"/>
            <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Ratings}" SelectedItem="{Binding Rating}" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Debut year:" VerticalAlignment="Center" Margin="0,6,10,6"/>
            <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding DebutDate}" />
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" >
            <Button Content="Back" Width="90" Margin="0,0,10,0" Command="{Binding BackCommand}"/>
            <Button Content="Save" Width="110" Command="{Binding AddArtistCommand}"/>
        </StackPanel>
    </Grid>
</Window>
